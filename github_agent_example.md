# GitHub Issue Research Agent - Example

## Original Issue (Vague)
```
Title: Add user preferences for notifications
Body: Users should be able to turn off email notifications. Make it configurable.
```

## AI Agent Process

1. **Reads the issue** using `github_read_issues`
2. **Analyzes repository** using `github_analyze_repository` 
3. **Researches code** using `github_read_code` on:
   - User models and schemas
   - Existing notification system
   - Settings/preferences components
   - Email service configuration
   - Similar preference features

4. **Updates the issue** using `github_update_issue` with this analysis:

---

## 🤖 AI Research & Implementation Guide

### Code Analysis Summary
After researching the codebase, I found:
- User model at `backend/models/User.js` with existing preference fields
- Email notification service in `backend/services/EmailService.js`
- Frontend settings page at `frontend/src/screens/SettingsScreen.tsx`
- Existing notification preferences for push notifications that can be extended

### Implementation Plan

#### Step 1: Update User Schema
- **Files to modify:** `backend/models/User.js`, `migrations/add_email_notification_preference.sql`
- **Changes needed:** Add `emailNotificationsEnabled` boolean field (default: true)
- **Code example:**
```javascript
// Add to User model
emailNotificationsEnabled: {
  type: Boolean,
  default: true
}
```

#### Step 2: Update Email Service
- **Files to modify:** `backend/services/EmailService.js`
- **Changes needed:** Check user preference before sending emails
- **Code example:**
```javascript
async sendNotificationEmail(userId, subject, body) {
  const user = await User.findById(userId);
  if (!user.emailNotificationsEnabled) {
    console.log('Email notifications disabled for user:', userId);
    return;
  }
  // Existing email logic...
}
```

#### Step 3: Add Frontend Settings
- **Files to modify:** `frontend/src/screens/SettingsScreen.tsx`
- **Changes needed:** Add toggle switch for email notifications
- **Dependencies:** None (using existing Switch component)

#### Step 4: Create API Endpoint
- **Files to create:** `backend/routes/userPreferences.js`
- **Purpose:** Handle preference updates from frontend
- **Dependencies:** Express route handler

#### Step 5: Update Frontend API Client
- **Files to modify:** `frontend/src/api/client.ts`
- **Changes needed:** Add method to update email preferences

### Testing Requirements
- [ ] Unit tests for EmailService preference checking
- [ ] Integration tests for preference API endpoint
- [ ] Frontend component tests for settings toggle
- [ ] Manual testing: Verify emails stop when disabled

### Acceptance Criteria
- [ ] Users can toggle email notifications on/off in settings
- [ ] Email service respects the user preference
- [ ] Preference is persisted across sessions
- [ ] Existing users default to notifications enabled
- [ ] Clear visual feedback when preference is changed

### Implementation Notes
- **Estimated effort:** Medium (spans frontend, backend, and database)
- **Risk factors:** Ensure existing email flows aren't broken
- **Related files:** 5 files to modify, 1 new migration, 1 new route
- **Database migration required:** Yes, for new user preference field

---
*This analysis was generated by AI after researching the codebase. Please review and adjust as needed.*

## Result
✅ **From:** "Add user preferences for notifications"  
✅ **To:** Complete implementation roadmap with 5 specific steps, file paths, code examples, and testing requirements

Engineers can now implement this feature immediately without any additional research! 