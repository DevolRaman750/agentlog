{
  "id": "func-team_task_delete-1754910004",
  "name": "team_task_delete",
  "provider": "internal",
  "display_name": "Team Task: Delete",
  "description": "Permanently delete a task from the team's task system. This removes the task and breaks any parent-child or dependency relationships. Use with caution as this action cannot be undone. Typically used for tasks created in error or duplicates.",
  "function_group": "internal",
  "endpoint": {
    "path": "/internal/team/task/delete",
    "method": "POST"
  },
  "parameters": {
    "type": "object",
    "properties": {
      "task_id": {
        "type": "string",
        "description": "Unique identifier of the task to delete"
      }
    },
    "required": [
      "task_id"
    ]
  },
  "examples": [
    {
      "name": "Delete a duplicate task",
      "parameters": {
        "task_id": "task_dup_999"
      }
    },
    {
      "name": "Delete a task created in error",
      "parameters": {
        "task_id": "task_err_001"
      }
    }
  ]
}