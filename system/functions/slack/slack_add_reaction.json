{
  "id": "00000000-0000-0000-0000-000000000087",
  "name": "slack_add_reaction",
  "provider": "slack",
  "display_name": "Slack Add Reaction",
  "description": "Add an emoji reaction to a message",
  "endpoint": {
    "path": "https://slack.com/api/reactions.add",
    "method": "POST"
  },
  "parameters": {
    "required": [],
    "optional": [],
    "schema": {"type": "object", "required": ["channel", "timestamp", "name"], "properties": {"name": {"type": "string", "description": "Emoji name (without colons, e.g., \\"thumbsup\\")"}, "channel": {"type": "string", "description": "Channel containing the message"}, "timestamp": {"type": "string", "description": "Timestamp of the message to react to"}}}
  },
  "examples": [
    {
      "name": "Basic usage",
      "parameters": {}
    }
  ]
}
