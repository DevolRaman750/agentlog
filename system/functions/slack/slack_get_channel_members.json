{
  "id": "00000000-0000-0000-0000-000000000084",
  "name": "slack_get_channel_members",
  "provider": "slack",
  "display_name": "Slack Get Channel Members",
  "description": "Get list of users who are members of a conversation",
  "endpoint": {
    "path": "https://slack.com/api/conversations.members",
    "method": "GET"
  },
  "parameters": {
    "required": [],
    "optional": [],
    "schema": {"type": "object", "required": ["channel"], "properties": {"limit": {"type": "integer", "default": 1000, "maximum": 1000, "minimum": 1, "description": "Maximum number of users to return (1-1000, default 1000)"}, "channel": {"type": "string", "description": "Channel ID to get members from"}}}
  },
  "examples": [
    {
      "name": "Basic usage",
      "parameters": {}
    }
  ]
}
