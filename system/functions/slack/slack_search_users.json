{
  "id": "00000000-0000-0000-0000-000000000091",
  "name": "slack_search_users",
  "provider": "slack",
  "display_name": "Slack Search Users",
  "description": "Search for users in the Slack workspace by name, email, or display name. Useful for resolving ambiguous user references.",
  "endpoint": {
    "path": "/users.list",
    "method": "GET"
  },
  "parameters": {
    "required": [],
    "optional": [],
    "schema": {"type": "object", "required": ["query"], "properties": {"limit": {"type": "integer", "default": 50, "maximum": 1000, "minimum": 1, "description": "Maximum number of users to return (1-1000, default 50)"}, "query": {"type": "string", "description": "Search term to find users (name, email, display name, or real name)"}, "include_locale": {"type": "boolean", "default": false, "description": "Include locale information for users"}}}
  },
  "examples": [
    {
      "name": "Basic usage",
      "parameters": {}
    }
  ]
}
