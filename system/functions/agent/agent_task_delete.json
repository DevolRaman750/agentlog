{
  "id": "func-agent-task-delete",
  "name": "agent_task_delete",
  "provider": "internal",
  "function_group": "internal",
  "display_name": "Agent Task Delete",
  "description": "Delete a task record from the agent's personal task tracking system. Used to clean up old task records, remove completed items, or delete incorrect entries from the agent's personal task storage.",
  "endpoint": {
    "path": "/internal/agent/task/delete",
    "method": "DELETE"
  },
  "parameters": {
    "type": "object",
    "required": [
      "task_id"
    ],
    "properties": {
      "task_id": {
        "type": "string",
        "description": "Unique identifier of the task to delete"
      },
      "context": {
        "type": "string",
        "default": "agent_tasks",
        "description": "Storage context/category where the task is stored"
      },
      "confirm_delete": {
        "type": "boolean",
        "default": false,
        "description": "Confirmation flag to prevent accidental deletions"
      }
    }
  },
  "examples": [
    {
      "name": "Delete reported task record",
      "parameters": {
        "task_id": "task-123",
        "context": "reported_tasks",
        "confirm_delete": true
      }
    },
    {
      "name": "Clean up old personal task",
      "parameters": {
        "task_id": "personal-789",
        "confirm_delete": true
      }
    }
  ]
}