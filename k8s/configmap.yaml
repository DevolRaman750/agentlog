apiVersion: v1
kind: ConfigMap
metadata:
  name: agentlog-config
  namespace: agentlog
data:
  PORT: "8080"
  # Add other non-sensitive variables here
  # Sensitive values (DB_URL, JWT_SECRET, API_ENCRYPTION_KEY, etc.) are now in agentlog-backend-secrets Secret 