apiVersion: v1
kind: Service
metadata:
  name: agentlog-backend
  namespace: agentlog
  labels:
    app: agentlog-backend
spec:
  type: ClusterIP
  selector:
    app: agentlog-backend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP 