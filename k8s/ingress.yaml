apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agentlog
  namespace: agentlog
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  rules:
    - host: agentlog.example.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: agentlog-backend
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agentlog-frontend
                port:
                  number: 80
  tls:
    - hosts:
        - agentlog.example.com
      secretName: agentlog-tls 