apiVersion: v1
kind: Service
metadata:
  name: agentlog-frontend
  namespace: agentlog
  labels:
    app: agentlog-frontend
spec:
  type: ClusterIP
  selector:
    app: agentlog-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP 